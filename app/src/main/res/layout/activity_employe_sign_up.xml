<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:background="@mipmap/background"
    android:orientation="vertical"
    tools:context=".employee.EmployeeSignUpActivity"
    android:weightSum="1">

    <EditText
        android:layout_marginTop="100dp"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textPersonName"
        android:id="@+id/employee_sign_up_name_edit_text"
        android:hint="First name"
        android:layout_marginBottom="20dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textPersonName"
        android:id="@+id/employee_sign_up_surname_edit_text"
        android:hint="Surname"
        android:layout_marginBottom="20dp"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textEmailAddress"
        android:id="@+id/employee_sign_up_email_edit_text"
        android:layout_marginBottom="20dp"
        android:hint="Email address"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textPassword"
        android:id="@+id/employee_sign_up_password_edit_text"
        android:layout_marginBottom="20dp"
        android:hint="Password"/>

    <EditText
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textPersonName"
        android:id="@+id/employee_sign_up_designation_edit_text"
        android:layout_marginBottom="20dp"
        android:hint="Designation e.g. Receptionist"/>

    <!--<EditText-->
        <!--android:layout_width="match_parent"-->
        <!--android:layout_height="40dp"-->
        <!--android:background="@drawable/rounded_edit_text"-->
        <!--android:inputType="textPostalAddress"-->
        <!--android:id="@+id/employee_sign_up_address_edit_text"-->
        <!--android:layout_marginBottom="20dp"-->
        <!--android:hint="Work address"/>-->

    <AutoCompleteTextView
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:background="@drawable/rounded_edit_text"
        android:inputType="textPostalAddress"
        android:id="@+id/employee_sign_up_address_edit_text"
        android:layout_marginBottom="20dp"
        android:hint="Work address" >

        <requestFocus />
    </AutoCompleteTextView>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="20dp"
        android:orientation="horizontal">

        <EditText
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:background="@drawable/rounded_edit_text"
            android:clickable="false"
            android:cursorVisible="false"
            android:focusable="false"
            android:layout_weight="1"
            android:focusableInTouchMode="false"
            android:hint="Select admin  "
            android:id="@+id/employee_sign_up_admin_edit_text"
            android:layout_marginBottom="20dp"
            android:layout_marginRight="10dp"/>

        <Button
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="Select"
            android:background="@drawable/rounded_button"
            android:textAllCaps="false"
            android:textColor="@android:color/background_light"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:id="@+id/employee_sign_up_admin_browse_button"/>
    </LinearLayout>

    <Button
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:text="Create an account"
        android:background="@drawable/rounded_button"
        android:textAllCaps="false"
        android:textColor="@android:color/background_light"
        android:textAppearance="?android:attr/textAppearanceMedium"
        android:id="@+id/employee_sign_up_save_button"/>

</LinearLayout>
